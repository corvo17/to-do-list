package com.corvo.demo.helper

enum class FrequencyList(var frequency: Int,val text: String) {
    EVERY_HOUR(1,"Every 1 hour"),
    EVERY_TWO(2, "Every 2 hours"),
    EVERY_THREE(3,"Every 3 hours"),
    EVERY_FOUR(4,"Every 4 hours"),
    EVERY_FIVE(5,"Every 5 hours"),
}